package com.cortex.app.core.ui.theme

import androidx.compose.ui.graphics.Color

// Cortex palette — editorial, dark, single-accent.
object CortexColors {
    val Ink = Color(0xFFEDEAE3)              // body text, off-white
    val Paper = Color(0xFF0E0E10)            // background, near-black
    val PaperRaised = Color(0xFF17171A)      // card surfaces
    val PaperSunken = Color(0xFF08080A)      // insets
    val Rule = Color(0xFF2A2A30)             // dividers
    val Muted = Color(0xFF6B6B73)            // secondary text
    val Accent = Color(0xFFD4FF4F)           // acid green, CTAs + progress
    val AccentDim = Color(0xFF8FA832)
    val Warning = Color(0xFFE8A447)
    val Danger = Color(0xFFE35A5A)
    val Success = Color(0xFF7FD67F)
}
